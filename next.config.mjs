/**
 * Next.js configuration.
 *
 * Kept intentionally minimal — the platform's complexity lives in
 * the application layer, not the build layer.
 *
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  reactStrictMode: true,
  poweredByHeader: false,
};

export default nextConfig;
