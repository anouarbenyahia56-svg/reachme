/**
 * Tailwind CSS v4 PostCSS plugin — replaces the @tailwindcss/vite
 * plugin used in the Vite era. Next.js picks this up automatically.
 */
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
